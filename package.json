{
  "name": "test-codex-chatgpt",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack demo ecommerce composed of a React front-end and an Express-like backend.",
  "scripts": {
    "format": "prettier --write .",
    "lint": "echo 'No root lint step'"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "prettier": "3.2.5"
  }
}
